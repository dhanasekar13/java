package task;

import java.util.Scanner;

public class dq {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		Scanner s=new Scanner(System.in);
		System.out.println("enter the string ");
		String r=s.nextLine();
		int hi=0,t=0,y=0;
		int ci=r.length();
		char[] d=new char[ci];
		char[] du=new char[ci];
		int g=0,q=0,j=0;
		    for (int i=0;i<ci;i++)
		{
			d[i]=r.charAt(i);
		}
		    du[0]=d[0];
          for(int i=0;i<ci;i++)
          {
        	if(du[0]==d[i])
        		g++;     	  
          }
          System.out.println("the number of elment "+du[0]+" conatins "+g);
          for (int i=1;i<=ci;i++)
          {  y=0;
        	  for ( j=0;j<du.length;j++)
        	  {t++;
          	 if(d[i]==du[j])
        	 {
          		  y=1;
        		 break;
        	 }  
          	 else
          	 {
          		 continue;
          	 }
        	  }
          	 if(y==0)
          	 {
          		du[i]=d[i] ;
          	 }
        	  
        	  System.out.println(du[i]);
        	  
        	  
        	  
          }
          
          
          
          
	}

}
